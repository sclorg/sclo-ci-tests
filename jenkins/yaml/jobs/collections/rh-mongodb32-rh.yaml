- project:
    name: rh-mongodb32
    namespace: rh
    release:
        - 7
    arch: x86_64
    repotype:
        - candidate
    jobs:
        - 'SCLo-{name}-{namespace}-C{release}-{repotype}-{arch}'
