- project:
    name: rh-python35
    namespace: rh
    release:
        - 7
    arch: x86_64
    repotype:
        - candidate
    jobs:
        - 'SCLo-{name}-{namespace}-C{release}-{repotype}-{arch}'
