- project:
    name: rh-nginx18
    namespace: rh
    release:
        - 6
        - 7
    arch: x86_64
    repotype:
        - candidate
    jobs:
        - 'SCLo-{name}-{namespace}-C{release}-{repotype}-{arch}'
