- project:
    name: %SCL%
    namespace: %NAMESPACE%
    release:
        - 6
        - 7
    arch: x86_64
    repotype:
        - candidate
    jobs:
        - 'SCLo-{name}-{namespace}-C{release}-{arch}'
